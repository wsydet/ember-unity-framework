# 模块名称：[填写模块名称]

---

## 1. 快速上手
> 用最少的代码展示本模块的典型用法，让使用者 30 秒内理解怎么调用。

```csharp
// [填写：一行注释说明这段代码做了什么]
var instance = [XxxManager].Instance;
instance.[核心方法]([参数], result => {
    // [处理结果]
});
```

---

## 2. 模块概述
> 用 1-2 句话说明本模块的核心职责，不涉及实现细节。

[填写：本模块负责什么，对外提供什么核心能力]

---

## 3. 依赖关系
> 列出本模块对外部的依赖，帮助评估升级/替换的影响面。

| 依赖 | 类型 | 说明 |
|------|------|------|
| `Ember.Core.Singleton` | 框架模块 | 单例基础设施 |
| `UniRx 7.1.0` | 外部包 | 响应式事件流 |
| [填写其他依赖] | | |

---

## 4. 文件清单
> 按类别列出本模块的工程文件，确保接手者能快速定位。

| 角色 | 路径 |
|------|------|
| 主逻辑入口 | `[Assets/Ember/Xxx/Runtime/XxxManager.cs]` |
| 核心数据类 | `[Assets/Ember/Xxx/Runtime/XxxData.cs]` |
| 关键接口 | `[Assets/Ember/Xxx/Runtime/IXxx.cs]` |
| 视图/表现层 | `[Assets/Ember/Xxx/Runtime/XxxView.cs]` 或 `[*.prefab]` |
| 编辑器扩展 | `[Assets/Ember/Xxx/Editor/XxxEditor.cs]` |

---

## 5. 公开 API

### 5.1 入口类型

| 类型 | 职责 | 获取方式 |
|------|------|----------|
| `[XxxManager]` | [模块门面，统一对外] | `XxxManager.Instance` |
| `[XxxData]` | [数据容器] | [由 Manager 创建 / 外部传入] |

### 5.2 核心方法

| 方法签名 | 说明 |
|----------|------|
| `[返回类型 方法名(参数)]` | [一句话描述] |
| `[返回类型 方法名(参数)]` | [一句话描述] |

---

## 6. 主流程
> 用箭头串联模块内部的主要执行路径。

**流程一：[名称，如 资源加载]**
`[外部调用]` → `[类A.方法()]` → `[校验]` → `[类B.加载()]` → `[回调返回]`

**流程二：[名称，如 资源释放]**
`[外部调用]` → `[类A.方法()]` → `[引用计数检查]` → `[卸载]`

---

## 7. 修改影响范围
> "要改 X → 去改 Y" 格式，快速定位修改点。

- **调整 [业务规则]**？ → 改 `[类名.方法名()]`
- **新增 [子类型/状态]**？ → 改 `[类名]` 中的 `[枚举/配置]`，同步更新 `[关联文件]`
- **调整 [UI/表现]**？ → 改 `[视图类名]` 或预制体上的 `[组件名]`

---

## 8. 约束与已知陷阱
> 只记不显而易见的，不写"命名要规范"之类的通用废话。

| 类别 | 说明 |
|------|------|
| 初始化顺序 | [必须等 Xxx 模块 Ready 后才能调用] |
| 生命周期 | [哪些资源需手动释放、哪些事件必须取消订阅] |
| 数据边界 | [有效值范围、空引用高发场景、缓存清理时机] |
| 已知问题 | [当前版本未修复的边缘 case 或临时 workaround] |
